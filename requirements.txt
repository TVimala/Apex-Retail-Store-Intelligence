# API
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2

# Database
sqlalchemy==2.0.35
aiosqlite==0.20.0
asyncpg==0.29.0          # optional: for PostgreSQL

# HTTP client (for pipeline → API live ingest)
requests==2.32.3
httpx==0.27.2             # used by test client

# Pipeline — Computer Vision
opencv-python-headless==4.10.0.84
numpy==1.26.4

# Detection model (install separately due to size; comment out for API-only usage)
# ultralytics==8.2.87     # YOLOv8 — pip install ultralytics
# supervision==0.22.0     # ByteTrack — pip install supervision

# Dashboard
rich==13.8.1

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0

# Dev utilities
python-dotenv==1.0.1
